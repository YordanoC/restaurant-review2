<%= simple_form_for([ restaurant, review ], remote: true) do |form| %>
  <%= form.input :content, as: :text , placeholder: 'Write a review',label: false %>
  <%= form.button :submit,class: "btn btn-success width-bottom-20" %>
<% end %>

